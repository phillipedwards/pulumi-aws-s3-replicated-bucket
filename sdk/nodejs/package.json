{
    "name": "@pulumi/aws-s3-replicated-bucket",
    "version": "${VERSION}",
    "scripts": {
        "build": "tsc",
        "install": "node scripts/install-pulumi-plugin.js resource aws-s3-replicated-bucket ${VERSION}"
    },
    "dependencies": {
        "@pulumi/aws": "^4.0.0",
        "@pulumi/pulumi": "^3.42.0"
    },
    "devDependencies": {
        "@types/node": "^14",
        "typescript": "^3.7.0"
    },
    "pulumi": {
        "resource": true,
        "name": "aws-s3-replicated-bucket"
    }
}
